---
title: "R/Python Exercise 1"
format: html
editor: visual
---

###### (Note to self: Installing packages in a code chunk won't make the document render, so you'll need to run it separately in the console.)

```{r}
library(pacman)

p_load(dlookr,
       DMwR2, 
       GGally, 
       Hmisc, 
       palmerpenguins, 
       tidyverse)
```
